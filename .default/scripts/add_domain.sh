#!/bin/bash

source ./const.sh


#
# ENRICH
#

#sudo cp "${tep_folder}/.default/nginx/nginx.conf" "/etc/nginx/sites-available/${ENRICH_HOST}.conf"
#sudo sed -i -e "s|{TEGIA_HOST}|$ENRICH_HOST|g" "/etc/nginx/sites-available/${ENRICH_HOST}.conf"
#sudo sed -i -e "s|{TEGIA_PORT}|$TEGIA_FCGI_PORT|g" "/etc/nginx/sites-available/${ENRICH_HOST}.conf"

#sudo ln -fs "/etc/nginx/sites-available/${ENRICH_HOST}.conf" "/etc/nginx/sites-enabled/${ENRICH_HOST}.conf"
#sudo sh -c -e "echo '127.0.0.1 $ENRICH_HOST' >> /etc/hosts";

